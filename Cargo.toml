[package]
name = "chladni-plates"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = {version="0.4.4", default-features = false}
rayon = "1.10.0"

[features]

default = ["plate"]

oscillations = []

plate = []
